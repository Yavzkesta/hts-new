---
// Pas besoin d'importer getAsset car nous n'allons pas référencer de fichiers CSS spécifiques
---

<style is:inline>
  /* Styles CSS critiques pour le rendu initial */
  :root {
    --aw-font-sans: 'Inter', sans-serif;
    --aw-font-serif: var(--aw-font-sans);
    --aw-font-heading: var(--aw-font-sans);
    --aw-color-primary: rgb(30 64 175);
    --aw-color-secondary: rgb(30 58 138);
    --aw-color-accent: rgb(109 40 217);
  }
  
  /* Styles de base pour le body et le contenu principal */
  body {
    font-family: var(--aw-font-sans);
    color: #1e293b;
    background-color: #fff;
    margin: 0;
    padding: 0;
  }
  
  /* Styles critiques pour le header */
  header {
    position: sticky;
    top: 0;
    z-index: 40;
    background-color: #fff;
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  }
  
  /* Styles pour le Hero */
  .hero-section {
    padding-top: 3rem;
    padding-bottom: 3rem;
  }
  
  /* Styles pour les titres principaux */
  h1 {
    font-size: 2.25rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1rem;
  }
  
  /* Mode sombre */
  .dark body {
    color: #f1f5f9;
    background-color: #050A1B;
  }
  
  .dark header {
    background-color: #050A1B;
  }
</style>

<!-- Préchargement des polices et ressources critiques -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap">
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" media="print" onload="this.media='all'">

<noscript>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap">
</noscript>

<!-- Script pour charger les styles de manière asynchrone -->
<script is:inline>
  // Fonction pour charger les styles de manière asynchrone
  (function() {
    const styleLinks = document.querySelectorAll('link[rel="stylesheet"][media="print"]');
    styleLinks.forEach(link => {
      link.onload = () => link.media = 'all';
    });
  })();
</script>